Use the Pico-W-Go Extension to connect to a Plasma2040 with the pimoroni micropython firmware.

main.py will run on the microcontroller

run `python host.py` to connect to a REST endpoint, get the color value, and then set the state of the neopixels.